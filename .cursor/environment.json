{
  "name": "Melt",
  "user": "root",
  "build": { "dockerfile": "Dockerfile" },
  "install": "npm ci && npx playwright install --with-deps chromium",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev"
    }
  ]
}
