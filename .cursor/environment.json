{
  "name": "Melt",
  "user": "root",
  "build": { "dockerfile": "Dockerfile" },
  "install": "npm ci\nnpx playwright install --with-deps chromium",
  "ports": [
    { "name": "web", "port": 5173 },
    { "name": "api", "port": 8787 },
    { "name": "anvil", "port": 8545 },
    { "name": "fixtures", "port": 8788 }
  ],
  "terminals": [
    {
      "name": "dev",
      "description": "Local Melt stack on a mainnet fork (chain id 31337): Anvil, API, fixtures, Vite. Open http://127.0.0.1:5173/app — no API keys required.",
      "command": "export PATH=\"$HOME/.foundry/bin:/root/.foundry/bin:/usr/local/bin:$PATH\"; MELT_FORK=1 npm run dev"
    }
  ]
}
